import promptSync from 'prompt-sync';
const prompt = promptSync();

/**
 * Desafio 8 – Calculadora Interativa
 * Realiza operações básicas (+, -, *, /) e avançadas, se desejar implementar (potenciação, raiz quadrada)
 * @param {number} num1 - Primeiro número.
 * @param {number} num2 - Segundo número (opcional para raiz quadrada).
 * @param {string} operacao - Operação a ser realizada: '+', '-', '*', '/'.
 * @returns {string} Resultado da operação ou mensagem de erro.
 */
export function calculadoraInterativa() {
	// Implemente sua solução aqui (obrigatório)
}

// ===== INTEGRAÇÃO COM O MENU PRINCIPAL =====
export function calculadoraInterativaPrompt() {
	console.log('\n=== CALCULADORA INTERATIVA ===\n');
	// Implemente sua solução aqui (opcional)
}

// ===== Testes =====
// Dica: Crie testes para sua função aqui abaixo e rode esse arquivo.
