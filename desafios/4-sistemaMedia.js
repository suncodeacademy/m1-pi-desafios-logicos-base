import promptSync from 'prompt-sync';
const prompt = promptSync();

/**
 * Desafio 4 – Sistema de Média com Situação
 * Calcula a média de um conjunto de notas e informa se o aluno está
 * Aprovado (>=7), em Recuperação (>=5) ou Reprovado (<5).
 *
 * @param {number} nota1 - Primeira nota numérica.
 * @param {number} nota2 - Segunda nota numérica.
 * @param {number} nota3 - Terceira nota numérica.
 * ou
 * @param {number} notas - Rest params de notas numéricas.
 *
 * @returns {string} - Mensagem com a média e a situação do aluno.
 */

export function sistemaDeMedia() {
	// Implemente sua solução aqui (obrigatório)
}

// ===== INTEGRAÇÃO COM O MENU PRINCIPAL =====
export function sistemaDeMediaPrompt() {
	console.log('\n=== SISTEMA DE MÉDIA ===\n');
	// Implemente sua solução aqui (opcional)
}

// ===== Testes =====
// Dica: Crie testes para sua função aqui abaixo e rode esse arquivo.
